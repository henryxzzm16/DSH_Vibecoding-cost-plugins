# Bundle patch layer: this row loads the package's main entry as the host half
# (a plain Cordis plugin); the `dsh.client` declaration in package.json makes
# the web app load ./client as the browser half.
- insert:
    - id: dsh-deepseek-cost
      name: dsh-deepseek-cost
